[agent]
harness = "codex"
args = ["--model", "gpt-5.3-codex"]

[repo]
path = "/absolute/path/to/your/repo"
default_branch = "main"

[tmux]
preset = "agent+tests"
startup_window = "agent"

[scripts]
before_start = [
  "scripts/init.sh",
]
cleanup = [
  "scripts/cleanup.sh",
]
